{
  "suit": "skyhighocs:helios",
  "requirements": [
    "skyhighocs:robot",
    "fiskheroes:tier8_9"
  ],
  "criteria": {
    "death": {
      "trigger": "fiskheroes:player_killed",
      "description": {
        "translate": "As Orion9450, die by collision with another player while both players are boost flying"
      },
      "condition": {
        "variables": {
          "player": "PLAYER_KILLED",
          "other_player": "DAMAGE_SOURCE"
          },
        "check": "player.getUUID() == '97f987c5-d983-4274-bbfa-26bdfa780562' && player.isWearingFullSuit() && other_player.getEntity().as('PLAYER').isWearingFullSuit() && player.getData('fiskheroes:flight_boost_timer') == 1 && other_player.getEntity().as('PLAYER').getData('fiskheroes:flight_boost_timer') == 1 && other_player.hasDamageType('COLLISION')"
      }
    }
  }
}